.DS_Store
node_modules
.worktrees
.sst
.env
.env.local
.idea
.vscode
.codex
*~
playground
tmp
dist
ts-dist
.turbo
**/.serena
.serena/
**/.omo
.omo/
/result
refs
Session.vim
/opencode.json
a.out
target
.scripts
.direnv/

# Local dev files
opencode-dev
# Mobile PWA <-> desktop sidecar dev binding. Machine-local and per-launch:
# committing it resurrects a dead port on every checkout, which is how the PWA
# used to end up bound to an unrelated opencode instance.
packages/mobile/.opencode-dev-handshake.json
packages/mobile/.opencode-dev-handshake.json.*.tmp
packages/mobile/.opencode-dev-url
UPCOMING_CHANGELOG.md
logs/
*.bun-build
tsconfig.tsbuildinfo

# Agent swarm databases (local-only, never commit)
.opencode/swarms/*.chunkdb*
# Unified file indexes now live in Global.data/file-index/ (SQLite + JSON).
# Legacy repo-local Brotli copy and any future .opencode file-index artifacts
# must stay untracked — they are derived caches, not source.
.opencode/file-index*
.opencode/*.br

# Local run artifacts (probe/e2e/profiling output)
*.log
*.cpuprofile
packages/app/cdp-screenshot.png
startup-investigation/raw/harness/cpu/
startup-investigation/raw/models-dev-api.json
startup-investigation/raw/trials.jsonl
startup-investigation/raw/backend-link-desktop-logs-mined.txt
startup-investigation/raw/*cpuprofile-summary.txt

workbuddy-usage-forensics.tar.br
*.tar.br
.opencode.json
