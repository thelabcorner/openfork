// Genspark model metadata lives in ./catalog.ts, which owns both the static
// fallback snapshot and the live /api/tool_cli/opencode-config fetch.
// This module remains the import path used across the package.

export * from "./catalog"
