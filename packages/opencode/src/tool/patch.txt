Deprecated alias — use `edit` instead. `edit` accepts this same patchText bulk pathway (multi-file/multi-hunk patches, dry-run by default, apply:true to commit) AND single precise edits, so prefer it for everything. This alias is kept working for back-compat; new calls should go to `edit`.
