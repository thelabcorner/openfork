// model search normalization/matching.
//
// The implementation moved to `@opencode-ai/schema/model-select/search` so the
// PWA model selector runs the exact same logic as this one instead of a copy
// that drifts. This module is kept as the renderer's import path.
export * from "@opencode-ai/schema/model-select/search"
