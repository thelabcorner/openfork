- Implemented the account-scoped WorkBuddy architecture: AccountRegistry discovers/deduplicates all valid desktop .info credentials by UID, AccountRouter binds sessions once, and each WorkBuddyAccount owns its own governor, auth refresh path, entitlement state, pacing, concurrency budget, queue, catalog, and metrics. Added account-qualified model aliases while preserving automatic `workbuddy/hy4-preview`.
- Governor now separates per-account active concurrency (configurable default 4), launch-rate token bucket, adaptive pressure, fair session-aware scheduling, and persisted entitlement states including WINDOW_LIMITED(resetAt) parsed from Tencent's observed frequency-limit message.
- Added offline tests: governor/state machine 34/34, multi-account registry/router 14/14, account-aware proxy 18/18, loopback proxy bypass 4/4; plugin bundle compilation succeeds. Wrote HY4_WORKBUDDY_FINDINGS.md.
- Corrected acquisition architecture: the official desktop `workbuddy-desktop.info` is additive import-only; OpenFork now owns a per-UID vault at `~/.workbuddy-ai/workbuddy/accounts/`, supports explicit desktop import plus Global/CN Tencent OAuth enrollment, persists account-local refresh updates, and retains accounts after simulated desktop switching. Enrollment mock test passes 10/10; updated multi-account test passes 18/18. Real UI account-switch and refresh-token revocation remain live-validation items.
- Hardened governor correctness: active leases now span SSE body EOF/cancellation rather than fetch headers; queued/active cancellation propagates; stable `x-opencode-request` logical keys reject duplicate in-flight generations; session routing accepts/injects all relevant OpenFork session headers; vault filenames use stable realm/enterprise/UID identities; credentials mutate in place; quota state uses non-secret enrollment epochs. New governor suite passes 40/40 and proxy suite 19/19.
- Deferred the real OpenFork Hy4 agent-session acceptance test because the account has an authoritative Tencent frequency-window limit; no additional live inference was issued. Local model registration and CLI startup were verified, and findings now record live e2e as unknown rather than failed.
